<Window x:Class="WpfApplication2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication2"
        xmlns:models="clr-namespace:WpfApplication2.Models"
        xmlns:vm="clr-namespace:WpfApplication2.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:MainWindowViewModel}"
        Title="{Binding WindowTitle}" MinHeight="430" MinWidth="508" Height="356.056" Width="427.155">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <StackPanel x:Name="OverrallPanel" Orientation="Horizontal" MinHeight="400" MinWidth="500">
        <StackPanel x:Name="FolderTreePanel" Height="400" Width="200">
            <Border BorderBrush="Gray" BorderThickness="1" Width="200" Height="400">
                
            </Border>
        </StackPanel>
        <StackPanel x:Name="FileViewPanel" Height="400" Width="300">

        </StackPanel>

    </StackPanel>
</Window>
